{
  "name": "measure-pressure",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/shared",
    "apps/api",
    "apps/app",
    "apps/doctor"
  ],
  "scripts": {
    "dev:api": "npm run build:shared && npm run dev --workspace=@mp/api",
    "dev:app": "npm run build:shared && npm run dev --workspace=@mp/app",
    "dev:web": "npm run build:shared && npm run web --workspace=@mp/app",
    "build:shared": "npm run build --workspace=@mp/shared",
    "build:api": "npm run build --workspace=@mp/api",
    "build:web": "npm run build:web --workspace=@mp/app",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:apply": "node scripts/atlas.mjs schema apply --env app",
    "db:plan": "node scripts/atlas.mjs schema apply --env app --dry-run",
    "db:inspect": "node scripts/atlas.mjs schema inspect --env app --format \"{{ sql . }}\"",
    "seed": "node scripts/seed.mjs",
    "dev:doctor": "npm run build:shared && npm run dev --workspace=@mp/doctor",
    "build:doctor": "npm run build:shared && npm run build --workspace=@mp/doctor"
  }
}
